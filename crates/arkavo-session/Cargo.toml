[package]
name = "arkavo-session"
version = "0.58.1"
edition = "2024"
authors = ["Arkavo Contributors"]
license = "Apache-2.0"
description = "Communication session management for the Arkavo agentic CLI tool - chat, websocket, HTTP sessions, and push notifications"
repository = "https://github.com/arkavo-com/arkavo-edge"
keywords = ["session", "chat", "websocket", "http", "communication"]
categories = ["network-programming", "web-programming::websocket"]

[dependencies]
# Core async runtime
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "net", "time", "sync", "signal"] }
async-trait = { workspace = true }
futures = { workspace = true }
tokio-stream = { workspace = true }

# WebSocket with TLS support
tokio-tungstenite = { workspace = true }
rustls = { workspace = true, features = ["std", "tls12"] }
rustls-pemfile = { workspace = true }
webpki-roots = { workspace = true }

# HTTP server
axum = { workspace = true }
tower = { workspace = true }
tower-http = { workspace = true }
http = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
anyhow = { workspace = true }
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }

# Database for session persistence
sqlx = { workspace = true }

# Utilities
uuid = { workspace = true }
chrono = { workspace = true }
dashmap = { workspace = true }

# LLM support for chat sessions
arkavo-llm = { path = "../arkavo-llm", default-features = false, features = ["kimi"] }

# Router for intelligent model selection
arkavo-router = { path = "../arkavo-router" }

# Temporary: re-export from arkavo-protocol until fully migrated
arkavo-protocol = { path = "../arkavo-protocol" }

[lints]
workspace = true
