[package]
name = "arkavo-llama-cpp"
version.workspace = true
edition = "2021"

[dependencies]
arkavo-llama-cpp-sys = { path = "../arkavo-llama-cpp-sys" }
libc = { workspace = true }

[dev-dependencies]
arkavo-test-macros = { path = "../arkavo-test-macros" }
# Pins the GGUF packer's transcribed quant table against ggml itself.
arkavo-gguf-tdf = { path = "../arkavo-gguf-tdf" }
criterion = { workspace = true }
glob = { workspace = true }

[[bench]]
name = "performance"
harness = false